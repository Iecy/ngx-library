import { Fullscreen.ToggleDirective } from './fullscreen.toggle.directive';

describe('Fullscreen.ToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new Fullscreen.ToggleDirective();
    expect(directive).toBeTruthy();
  });
});
