import { Fullscreen.SwitchDirective } from './fullscreen.switch.directive';

describe('Fullscreen.SwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new Fullscreen.SwitchDirective();
    expect(directive).toBeTruthy();
  });
});
