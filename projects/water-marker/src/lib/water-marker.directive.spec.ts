import { WaterMarkerDirective } from './water-marker.directive';

describe('WaterMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new WaterMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
